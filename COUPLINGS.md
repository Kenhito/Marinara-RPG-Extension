# Coupling Ledger

Every place MRR touches a Marinara Engine surface outside the documented, versioned public API. Class A = documented 2.4.3 API · B = REST route · C = engine internal (undocumented, could shift on any engine release). Every Class-C row needs a feature probe, a graceful fallback, and a visible degradation warning in the MRR panel status area.

**The Coupling Law (non-negotiable):** any commit that adds/removes/changes an engine touchpoint updates this file in the same commit. See `AGENTS.md` §8.5.

Rows marked `PENDING — live probe` were seeded from the plan's design/static analysis this session (agent-executed, no live engine access) and need a follow-up live probe on the maintainer's running engine (plan §4.A0.3–A0.5) before they can be marked confirmed.

## Ledger discipline

`confirmed` is a claim about a moment in time, not a property of a coupling. This ledger treats it that way.

**Every `confirmed` row must carry both of these, in the Status cell, or it is not confirmed:**

1. **The verification DATE** — when a human or a probe actually observed the behaviour.
2. **The engine version it was verified against** — the tag, release, or `HEAD <version>-dev` string. "Latest" is not a version.

A row that names neither is `unverified`, whatever its cell currently says, and must be re-probed before anything is built on it.

**Rows expire on any engine minor bump.** When the engine moves 2.4.x → 2.5.0, every `confirmed` row whose recorded version is older than the new minor drops to `stale-recheck` until someone re-verifies it. This is mechanical and not a judgement call — do not exempt a row because it "obviously still works." Patch bumps (2.4.3 → 2.4.4) do not expire rows on their own, but any row whose probe touches a surface named in that patch's changelog does.

**Why this rule exists — the round-21 truth-pass.** Round 21 re-read the engine source behind rows this file already marked `confirmed` and found **three of them false**:

- Row 2 — "a swipe's fresh generation (regenerate) DOES run agents, same pipeline, no special-casing." False, and false at every version back to 2.0.9: `shouldRunPreGen` is gated on `!input.regenerateMessageId`. A whole feature (B2-R) had been specced against a generation shape that does not occur.
- Row 3 — "agent `type` can be migrated in place via PATCH." False: `agentsStore.update()` has no `type` field at all; the PATCH is accepted and the type silently ignored.
- Row 3 — the probe cited as proof of that migration (R20-F1-2) had **never asserted the thing it was cited for**. It passed while the claim it supported was false.

None of those were careless entries. Each was written by someone reading real source, and each was true of some *other* code path, some *other* lane, or some *earlier* version. That is exactly the failure mode a date and a version number catch and that confidence does not: the claim did not rot because it was sloppy, it rotted because the engine moved and nothing in the ledger forced anyone to look again.

**A corollary the same pass earned:** when a row is corrected, retract the old claim *in place* and say so loudly (`⚠️ RETRACTED`), rather than quietly rewriting it. Three of this file's most load-bearing cells carry retractions today, and the retraction is the most useful thing in them — a future reader needs to know the shape of what we believed, or they will re-derive the same wrong thing from the same plausible reading.

**Status legend:** `confirmed <date> — <engine version>` (verified, in date, against a named version) · `stale-recheck` (was confirmed; expired by an engine minor bump, needs re-probe) · `PENDING — live probe` (derived from static analysis only; never observed running) · `unverified` (asserted with no date, no version, or no probe) · `RETRACTED` (proven false; the row keeps the retracted text so the wrong belief stays visible).

| # | Coupling | Class | Where (symbol) | Probe | Fallback / degradation | Status |
|---|---|---|---|---|---|---|
| 1 | `marinara` full-page API (fetch/storage/timers/onCleanup/log/extension) | A | compat shim, `extension/RPG-Extension-GM-Mode.js` (top of file, after constants) | `typeof marinara.fetch === "function"` (feature-detects legacy vs 2.4.x host inside the shim) | pre-2.3.4 host object passthrough (shim returns `host` unchanged when `host.addElement` exists) | Confirmed 2026-08-22 — `FullPageExtensionApi` v1 shape verified against engine `v2.4.3` tag (`PersonalExtensionInjector.tsx`) |
| 1b | `marinara.storage.get()/.patch(obj)/.clear()` (B1 — server-backed character/pref state) | A | B1 storage adapter, `extension/RPG-Extension-GM-Mode.js` ~line 273 (`hasServerStorage` feature-detect, `hydrateStore`, `lsGet`/`lsSet`/`lsDel` wrappers) | `!!(marinara.storage && typeof marinara.storage.get === "function")`, one `.get()` at startup | absent/failed `.get()` → `hasServerStorage=false` for the session, all reads/writes fall through to plain `localStorage` exactly as pre-B1; one console warning | Confirmed 2026-08-22 against engine source, CORRECTED round 5 (an adversarial review caught the API-surface citation below was backwards): the FULL-PAGE lane — `PersonalExtensionInjector.tsx:36-51` (`FullPageExtensionApi`), which is the runtime this row and this loader actually run under (row 1) — exposes `storage: { get(), patch(value), clear() }`; client-side `clear()` issues `DELETE /storage` under the hood (`PersonalExtensionInjector.tsx:286-288`), which server-side calls `settings.remove` (whole-record wipe). The earlier `.delete()` citation (`personal-extensions.routes.ts:665-669`) is a DIFFERENT lane — the sandboxed-WORKER API (`version: 5`) that this loader does not use. Both lanes ultimately wipe the whole row the same way; the method is just named differently per lane. `patch` is a server-side shallow merge `{...current, ...patch}` (`personal-extension-settings.service.ts:27-31`), re-validated as `z.record(z.string(), z.unknown())` with a 1,000,000-byte UTF-8 cap on the MERGED record (900,000-byte client-side guard kept under that). `patch({k: null})` STORES `null` — treated client-side as a tombstone (present-but-absent on read; migration never resurrects a tombstoned key from `localStorage`). `.clear()` removes the ENTIRE extension record — **forbidden**, never called from this loader (nor is the worker lane's `.delete()`); per-key deletion is `patch({key: null})`. The gate's forbidden grep is `storage\.(delete|clear)\(|apiFetch\("/extensions/` (both wipe-methods covered, whichever lane a future engine version routes us through). Storage layout is one record, one top-level key per `localStorage` key string (never nested) — the shallow merge is what keeps that key-shape from getting clobbered by a patch touching a different key; it is NOT, by itself, safe against concurrent writers. `patch` server-side is an UNSERIALIZED read-modify-write (`personal-extension-settings.service.ts:27-31`: `await get(extensionId)`, then `{...current, ...patch}`, then `await appSettings.set(...)` — three await points with no lock between the read and the write) — last-writer-wins on the WHOLE row if two patches race in that window (see finding 10 / multi-tab note at `mrrStampTsMap`). Mitigated CLIENT-side, not server-side: newest-wins re-adoption at the next hydrate (`mrrHydrateMerge`) and on tab-refocus (`mrrReconcileFromOtherTabs`, FIX-3) re-derive the correct value from localStorage's ts-map rather than trusting whichever patch happened to land last. `localStorage` is retained as a current local mirror this release (not deletable fallback only); failed background patches retry with backoff then quarantine the key to local-only for the session (round 5 FIX-6: an over-budget batch now quarantines only the character-family keys and retries excluding them, so small keys aren't held hostage by one oversized character; round 5 FIX-5: the local ts-map stamp always happens on write, even while quarantined, so a quarantined edit isn't silently discarded by newest-wins at the next hydrate). Synced key set (coordinator RULING 1, validation round 1): character sheets (`mrr-character-*`), the active ruleset id, spellbook lorebook-id caches, AND the per-chat character roster (`mrr-chars-<chatId>`)/active-character pointer (`mrr-active-char-<chatId>`) — the latter two added because the design's own cross-browser verify criterion is unreachable without them. **B19 addition (2026-08-24):** `mrr-last-char-<rulesetId>` — the last-character-per-ruleset continuity pointer, `{"id","name"}`, stamped from the single activation chokepoint `saveActiveCharacterId()` and gated on the round-24 latch (never stamped from an unconfirmed chat, so the pointer cannot be filed under the wrong ruleset). Keyed by ruleset id, NOT chatId — being global per ruleset is the whole point, since it is what lets a rotated/new chat offer to continue as the character last played in that system. Read-only consumer is the explicit "Continue as &lt;name&gt;" panel offer (`mrrRenderContinueOffer`), which never auto-adopts and whose decline path performs zero writes. Requires no `mrrRunMigration` task (the key has no legacy instances; every write goes through `lsSet`/`mrrMarkDirty` normally). Probe coverage: `Plans/probe-harnesses-20260824/b19-continuity-probes.mjs` (10 probes, eval-extracted, P7 negative control). Degradation is now VISIBLE, not console-only (coordinator RULING 2): a lazily-created `div.mrr-warn-strip`, mounted at the top of the real main sheet panel content root (`state.mountEl`, built inside `mrrP3RenderSheet`) on every render and live-updated in place for warnings fired mid-session, shows the most recent message (+N-more count for multiple queued), with a session-local × dismiss that a new distinct warning clears. **TIMESTAMPED ENVELOPES + NEWEST-WINS MERGE (maintainer override, 2026-08-22, coordinator validation round 4 — SUPERSEDES the round-1/07-15-plan "deterministic, no timestamps" precedence):** crash recovery must never resurrect older character data, so every synced-key value stored server-side is now an envelope `{"t": <epoch ms>, "v": <raw string value>}`; a tombstone is `{"t": <epoch ms>, "v": null}` — deletions are timestamped too. A server value present but not shaped like an envelope (no finite numeric `t`, or `v` not a string/null) is read defensively — round 5 FIX-11 tightened this: a bare raw STRING normalizes to `{t: 0, v: <that string>}` (the data is real, keep it), but anything else at all (a foreign object, number, bool, or an envelope-shaped object whose `t` is non-finite or whose `v` isn't a string/null) normalizes to `{t: 0, v: null}` — treated as genuinely absent, LS fallback serves. `lsGet` is guaranteed to never return a non-string, non-null value. `localStorage` itself is untouched (still raw strings); the LS-side timestamp lives in one local-only sidecar, `mrr-ts-map` (`{<syncedKey>: <epoch ms>}`, written via `lsSetRaw`) — deliberately **not** itself a synced key, since syncing it would let the engine's shallow `patch` merge whole-map-clobber another browser's independently-touched keys. `mrrHydrateMerge` runs on **every** hydrate (unlike the one-time-flag-gated migration) and, for every synced key found in either store, adopts the LS value into the cache (re-flushing it stamped with its **original** `lsTs`, never a fresh `now()`) whenever `lsTs` is strictly greater than the server envelope's `t` (0 for absent/malformed); otherwise the server envelope wins as-is, including tombstones (a newer tombstone stays deleted even over an older LS copy; a strictly-newer LS write recreates over an older tombstone). Migration itself now stamps a freshly-copied legacy value with its `mrr-ts-map` entry when one exists, else `t: 0`. `now()` source is plain `Date.now()`; cross-browser clock skew is accepted (no monotonic counter). Probe coverage: `b1-storage-probes.mjs` P10-P14 (envelope round-trip, crash-adopts-and-preserves-original-ts, server-newer-wins, tombstones both directions, malformed-value defensive read) and `b1-roundtrip-probes.mjs` R1/R2/R4a/R4b (updated for envelope shape) in `/tmp/claude-1000/-home-kenhito/cd8769f7-02f3-4171-9e4f-4c79414295b1/scratchpad/phase2-probes/`. **ROUND 5 ADDITIONS:** FIX-3 (major, multi-tab stale cache) — the in-memory cache never refreshed on its own while `localStorage` is shared/current across every tab in the same browser; `mrrReconcileFromOtherTabs` now runs on `visibilitychange`→visible (same `watchLifecycleSaves` registration as the existing flush listeners) and adopts any ts-map key newer than the cache holds, via the SAME `mrrAdoptLsIfNewer` helper `mrrHydrateMerge` uses — but does NOT mark the adopted key dirty (the other tab already owns pushing it server-side; re-flushing here would race that tab's own in-flight patch against the RMW hazard described above, for no benefit). FIX-8 (major, hung `get()` bricks init) — `hydrateStore()` races `marinara.storage.get()` against an 8000ms `marinara.setTimeout`; a late real response after the timeout has already won is inert by construction (nothing is chained onto the original `get()` promise, only onto the race result). Accepted, not fixed: finding 12 (stale-cache projection) — the size guard measures the CACHE's projected record, which can drift slightly stale relative to the true server row between hydrates; the margin (900,000 of 1,000,000 bytes, ~10%) is judged sufficient headroom for that drift and is not being tightened further this round. |
| 2 | `GET /agents/runs/:chatId/custom` | B | `pollCustomAgentRuns`; run→assistant re-anchoring via `mrrBuildMsgAnchorMap`/`mrrResolveRunDomAnchor`/`mrrIsHiddenFromUser` (round 18) | A0 live GET | poller warns once, narrator path unaffected | **Confirmed live 2026-08-22** — dump stage captured real run rows (shape: `resultData.text`). ⚠️ **The original "`messageId` == narrator `data-message-id`" half of this line was WRONG and is retracted — see the round-18 addendum at the end of this cell.** It was a single dump-stage eyeball, never audited against the engine's save sites, and it is what let the orphaned-bucket defect survive seventeen debugging rounds; apply stage live-proven (health/essence/anima mutations). Poller mode now `"apply"` — this transport is the PRIMARY state channel. **Round-11 T1 update — row shape + sole-writer filter, both live-confirmed:** read `agents.storage.ts` directly (`listCustomRunsForChat` → `agentRuns.innerJoin(agentConfigs)` → `serializeRunWithConfig`) — every row is FLAT: `{id, agentConfigId, agentType, agentName, chatId, messageId, resultType, resultData, tokensUsed, durationMs, success, error, createdAt}`; `agentConfigId` is the joined `agent_configs.id`, `agentName` its display name — both present on every row already, no per-row lookup needed. the maintainer's live smoke test caught the exact failure this transport enables: with 7 managed agents enabled, an `applied 7/9` poll log meant ≥3 NON-mutator agents had echoed the same essence-spend tag with different `reason=` wording (different content sigs → triple-apply, -15 instead of -5) — the cross-transport dedup gate only catches byte-identical duplicates, not "three different-looking tags expressing one intent." Fix: `pollCustomAgentRuns` now applies state tags ONLY from the run whose `agentConfigId` matches the resolved managed state-mutator's agent-config id (`mrrResolveMutatorConfigId`, matched via `settings.mrrAgentRole === "state-mutator"` — the installer stamps this on every additionalAgent at install time; NAME is not robust, it varies per ruleset). A non-mutator run carrying `[mrr-state:]` text warns once per offending agent name and is marked processed WITHOUT applying/journaling. If the mutator's id can't be determined (managed install missing, `/agents` fetch fails, foreign/unmanaged agents), the filter falls back to the pre-T1 unfiltered behavior with its own one-time warn — never silently drops every run. **Round-11 T2b addendum (SUPERSEDED round 12, F2 — kept for history):** `pollCustomAgentRuns` used to stamp `mrrMutationJournal.lastSeenIdx[mid]` itself, bare, whenever a run's messageId/swipe-index were resolvable — without it, a message whose mutations arrive ONLY through this transport (the common case; the mutator's tags never land in chat DOM) never got a `lastSeenIdx` baseline for `processChatMessage`'s swipe-change comparison to work from, so revert silently never fired even once the narrator-side gate was fixed to reach that code at all. Round 12's F2 absorbed this bare stamp into the shared `mrrHandleSwipeTransition` (both transports now call the SAME function, which reverts-then-stamps atomically, closing the poller-wins-the-race wipe bug the bare stamp alone couldn't) — see the F2 CHANGELOG entry. **Round-14 Task B — engine staging semantics, precisely verified against `~/Marinara-Engine` source (not assumed):** `generate.routes.ts:929` `moveToActiveAgentRuns(messageId, swipeIndex)` — called at `:7868`/`:10152`, the instant the assistant message becomes visible (`assistant_message_ready` sent), NOT at "generation end" broadly — moves the request's generation record from a single-slot blocking map (`activeGenerations`, one entry per chat, blocks a NEW generation from starting) into a per-chat Set (`activeAgentRuns`, can hold multiple concurrent entries) purely so the user can start a new turn/swipe without waiting for background post-processing agents to finish. **This is in-memory bookkeeping only — it does NOT gate when a run becomes visible to us.** The actual DB write our poller reads (`agentsStore.saveRun`, `agents.storage.ts:317` `db.insert(agentRuns)`) is called synchronously/awaited at `generate.routes.ts:8492`, immediately upon each post-processing agent's own completion, in the SAME request continuation — there is no "staged until next-turn-commit" delay for write visibility; the original staging hypothesis (verbatim: "only moved to the queryable store at next-turn commit") does not hold as a general statement and is corrected here. What DOES happen at next-turn commit (`:986-1000`, gated by an actual NEW user-authored message — `input.userMessage`/attachments/pendingSpatialTransition — NOT by every regenerate) is a cancellation sweep: for each `activeAgentRuns` entry whose `(messageId, swipeIndex)` no longer matches the message's current `activeSwipeIndex`, `run.abortController.abort()` fires (`:998`, logged at `:993-997` "Cancelling work for abandoned swipe"). This is coarser than a hard kill: `executeAgent` (`agent-executor.ts:723`) accepts no `AbortSignal` parameter at all, and the post-processing save loop (`:8409-8500`) has no per-iteration aborted-check — an agent already executing or already reaching its `saveRun` call when the abort fires completes and saves regardless; the signal is only honored at a few coarse checkpoints (e.g. the `!abortController.signal.aborted` guard gating entry to the whole post-processing block at `:8164`). Net effect for us: usually LESS abandoned-swipe noise to dedup (good, as hypothesized) but not a hard guarantee. A swipe's fresh generation (regenerate) DOES run agents — confirmed, same pipeline, no special-casing. **Genuine, unfixable-from-our-side gap found during this verification:** `agent_runs`' schema (`db/schema/agents.ts:22-37`) has NO swipe-index column — only `messageId`. The engine internally pins which swipe a slow agent's result belongs to (`targetSwipeIndex`, `generate.routes.ts:~8410`, captured ONCE at save time specifically so a mid-flight swipe doesn't misattribute — see that block's own comment) but never persists that pin onto the row we read. Our `swipeMap`-at-apply-time resolution (`mrrHandleSwipeTransition`) already handles the COMMON, sane case correctly — a run finishing "late" while the CURRENT swipe is STILL the one it was computed for applies cleanly into that (still-current) bucket, no special handling needed, because the shared transition function already no-ops when `priorIdx === newIdx`. The unresolvable case — a run computed for a swipe the user has SINCE abandoned via regenerate-to-regenerate (which the engine's own cancellation, being gated to real user turns, does not catch) landing after the fact — has no fix available to us: we cannot know it wasn't for the current swipe, so it applies into whatever bucket `swipeMap` reports as current at that moment. Accepted, documented risk, same class as the Finding 7c journal-key-drift note above. **Round-16 addendum (BUG-1, journal-lifecycle hole, confirmed root cause of D&D's total swipe-revert silence):** `pollCustomAgentRuns` had no guard ensuring `mrrMutationJournal` was loaded/scoped for its `chatId` before journaling — reachable via a real startup-window race (`state.chatId` briefly null while Marinara initializes → `mrrMutationJournalChatId` gets explicitly nulled → poller fires with a now-valid chatId before the real per-chat load runs → a bucket is created in memory, `mrrSaveMutationJournal` silently no-ops on the still-null chatId, and the SUBSEQUENT legitimate load then wipes the bucket entirely). Fixed: `pollCustomAgentRuns` now calls `loadProcessedMessageIds(chatId)` unconditionally at the top of every poll cycle (idempotent when already loaded). See the CHANGELOG round-16 entry and `b2r-revert-probes.mjs` B1-1/B1-2/B1-3 for the full trace + negative control proving the fix is load-bearing. **Both round-16 retest sessions ran in RP mode; Game mode was NOT exercised against this fix and remains a future-verify item** — `processChatMessage`/`watchChatMessages` have no mode branch (see row 15's own note), so this fix should apply uniformly, but that has not been live-confirmed in Game mode. **Round-17 addendum:** round-16's fix protected ONLY this poller path — round-17's audit found `processChatMessage`'s own async apply callback (row 10) was NEVER given the equivalent protection (it only checked `state.chatId !== scanChatId`, never whether the journal had actually been loaded for that chat), which is the leading candidate for round-17's continued evaporation (EX3's evidence is specifically the DOM/narrator path, not this poller). Fixed symmetrically — `processChatMessage` now also calls `loadProcessedMessageIds(scanChatId)` before applying. This poller path also gained an explicit verify-and-self-heal check right before its own apply (warn + reload on any `mrrMutationJournalChatId !== pollChatId` mismatch) as a backstop, even though static analysis could not construct a live sequence where it fires given the current call graph — see the CHANGELOG round-17 entry for the full trace and the three new instrumentation log lines (journal save log, swipe-detect's jChat=/jBuckets= fields, mrrLoadMutationJournal's loud wipe-warn) added to make the next live log decisive. **Round-18 addendum (BUG-2, ORPHANED BUCKETS — the actual root cause; supersedes this row's original `messageId` claim):** `agent_runs.messageId` is bound **per agent phase**, not uniformly to the assistant message, and the split is **phase-vs-phase, NOT roleplay-vs-game** (no `chatMode` guard exists on any of the blocks below — verified by indentation scan across `:5032-5215`, `:7896-7925`, `:8164-8500`, `:9991-10030`). Traced at `~/Marinara-Engine` HEAD 2.4.4-dev, every `agentsStore.saveRun` call site read (9 sites, 4 files): custom **`pre_generation`** agents anchor to the **triggering USER message** (`generate.routes.ts:5197-5215` — `const latestUserMessageForPreGenRun = [...allChatMessages].reverse().find((m) => m.role === "user")`, then `messageId: preGenRunMessageId`; the loop explicitly skips built-ins, so these are exactly the rows this endpoint serves); custom `parallel`/`post_processing` agents anchor to the **assistant reply** (`:8414` `const messageId = (lastSavedMsg as any)?.id ?? ""`, consumed at `:8442`/`:8486`); custom lorebook read-behind agents anchor to a deliberately **historical** older message (`:4858-4862`, `:8276`). Every MRR ruleset installs its State Mutator as `phase: "pre_generation"` (all sixteen `bundle.json` files), so EVERY mutator run this poller reads carries the user turn's id. Because `processChatMessage` discards any node lacking the `mari-message-assistant` class before its bucket check (row 10), every bucket this transport wrote was structurally invisible to swipe detection — revert impossible, and no diagnostic line even fired. That is retest-7's D&D shape exactly (6 healthy buckets saved under the correct chat key; every observed DOM message `skipped — no tags, no buckets`). EX3 masked the same defect because its narrator emits inline tags, so its DOM path produced correctly-keyed buckets alongside its equally-orphaned poller ones. **Provisional-id hypothesis ruled out:** message ids are server-generated inside the insert (`services/storage/chats.storage.ts:1299-1311`) and never rewritten; runs always carry the real row id. **Fix (round 18):** the poller re-anchors BOTH its journal bucket key AND its cross-transport dedup anchor — necessarily the same string, since `mrrRevertBucket` derives the dedup anchor from the bucket key via `mrrAnchorFromBucketKey` — to the nearest **visible assistant** message at or after `run.messageId`, resolved by `role` (never by a hard-coded phase table, so this survives the engine moving which phase anchors where). A run already anchored to a visible assistant resolves to itself (no-op for the post_processing case). This matches the engine's own convention for pre-gen runs: `:7896-7900` anchors built-in Narrative Director pre-gen runs to "the first saved assistant message from this turn" — it simply never applied that to CUSTOM pre-gen agents. Assistant messages with `extra.hiddenFromUser: true` (the command-only anchor, `:7182-7197`) are skipped as candidates — `ChatRoleplaySurface.tsx:2231` never renders them, so they carry no `data-message-id` and would orphan a bucket just as thoroughly. **Second defect closed by the same change:** with mismatched anchors, an inline narrator tag and the mutator's identical run tag were anchoring different turns at each other, so they DOUBLE-APPLIED instead of deduping — the FIX-1 invariant (row 15) had been silently broken for this transport all along. **New dependency this row now carries:** the `role` field on `GET /chats/:chatId/messages` rows (row 15's endpoint, same request, no extra call — see `mrrBuildMsgAnchorMap`). Degradation: a response with no `role` on any row marks the anchor map unusable and the poller keeps exactly its pre-round-18 raw-messageId behavior with one warn (deliberately NOT a deferral). A run whose message is the newest in the chat with no reply yet — the NORMAL state for a pre-gen run, since `:5205` persists it strictly before `:7354` creates the assistant row — is left unprocessed and retried next poll, bounded by `MRR_RUN_ANCHOR_MAX_DEFERRALS = 200` (~5 min), after which it applies under the raw anchor with a warn rather than being lost (an orphaned bucket still mutates correctly; a dropped one does not). Probes: `b2r-revert-probes.mjs` R18-A1/A2/A3/A4, R18-B1/B2, R18-C1, R18-D1/D2/D3/D4/D5, R18-E1/E2/E3 plus three mechanically-derived negative controls (R18-B1-NEG, R18-B2-NEG, R18-C1-NEG). **Still true and still unfixed-from-our-side:** `agent_runs` has no swipe-index column (see this row's round-14 addendum above) — the re-anchoring fixes WHICH MESSAGE a run belongs to, not which swipe of it. **Round-19 addendum (BUG-3, poll-latch liveness — not an engine coupling change, but it governs whether this transport runs at all):** retest-8 proved the round-18 re-anchoring works at the detection layer (`swipe-detect: ... buckets=true idx=1 jBuckets=7` — a bucket found on a real DOM assistant message at a nonzero swipe index), but the poller then went permanently silent after its first apply. Cause: `runsPollInFlight` is set at the top of `pollCustomAgentRuns` and cleared only in the promise chain's terminal handler, and two paths reach neither — a request that never settles (this poller has NO timeout anywhere, unlike `hydrateStore`'s round-5 FIX-8 8000ms race), and a synchronous throw in the prologue between the latch being set and the promise being handed off (`loadProcessedMessageIds`/`loadProcessedRunIds` both touch storage — round 16 added the first of those to that exact window — and a synchronously-throwing `apiFetch` lands there too). Both reproduced (probes R19-S1-NEG/S2-NEG). Fixed with a 30s stall deadline that force-releases the latch on the next tick plus a try/catch around the synchronous prologue, with a monotonic cycle token so a zombie cycle can neither clear a newer cycle's latch nor apply stale results. **Endpoint ordering re-verified rather than assumed while chasing this:** `listCustomRunsForChat` (`agents.storage.ts:410-431`) orders `desc(createdAt)` and clamps limit to 200, so the NEWEST runs are always in our `?limit=50` window — a long chat can never scroll new runs out of view. **The round-18 defer branch is exonerated by test, not argument:** a deferred run stays unprocessed, so it re-enters the candidates list every poll, which forces a fresh `fetchSwipeIndexMap` and therefore a freshly rebuilt anchor map each cycle; three consecutive turns each defer-then-apply correctly (probe R19-M1). Deferral is now audible: one log line per poll cycle with at least one deferral, and the 200-deferral backstop warns on EVERY trip. **⚠️ ROUND-21 CORRECTION — the round-14 claim "a swipe's fresh generation (regenerate) DOES run agents — confirmed, same pipeline, no special-casing" is RETRACTED. It is false, and was false at every version back to 2.0.9.** Verified at source this round: `const shouldRunPreGen = (hasPreGenAgents OR reviewedAgentInjections.length > 0) && !input.regenerateMessageId;` (`OR` here stands for the logical-or operator in the source line) (`generate.routes.ts:4965`), and that flag is the sole gate on the entire pre-generation block (`:5032`); the sibling knowledge-router gate carries the same `!input.regenerateMessageId` condition (`:4960-4964`). **Pre_generation agents do NOT run on a regenerate/swipe.** The turn's previously cached `extra.contextInjections` are replayed instead, so the narrator still sees agent context while NO FRESH RUN IS CREATED. Consequences, stated only as far as the evidence reaches: a swipe normally yields no new custom-agent run for this poller to read, so this transport cannot be the per-swipe mutation signal — inline narrator tags on the DOM path (row 10) are the only per-swipe signal that exists. This does NOT invalidate round-18's re-anchoring (which governs where a run that IS created gets journaled) or round-19's latch fixes. It does mean B2-R was specced against a generation shape that does not occur; re-deriving that model is queued for the replan, not patched here. Round-11's "essence spends are static between swipes" ruling now reads as a design intent that happened to coincide with engine behaviour rather than a workaround for it. |
| 3 | `/agents` CRUD + `/agents/:id` | B | installer/reconciler family | A0 live GET | install aborts with panel error | Confirmed live 2026-08-22 — agent installs + prompt-sync PATCHes succeeded (7 managed agents). **Round-12 F1 update:** `reconcileActiveAgents` now takes a `rebind` argument. Passive call (`watchRouteChanges`' chat-switch handler, no arg) is unchanged — a chat stamped for a different ruleset (`meta.mrrChatRulesetId`) is still skipped with a warn, no PATCH. Active call (`init()`, right after `"activated ruleset ..."` — a deliberate user act) now passes `rebind: true`: on a stamp mismatch it computes both the OLD and NEW ruleset's managed agent ids from the SAME single `/agents` fetch already in flight, PATCHes `/chats/:id/metadata` with `activeAgentIds` = (existing minus old-managed) union new-managed plus the updated `mrrChatRulesetId` and `enableAgents: true`, and logs the exact `−N/+M` id-count delta. Live evidence this closes: `"activated ruleset dnd5e on chat X"` immediately followed by `"bound to ruleset exalted3e — skipping"` with no rebind path anywhere in the file before this fix. **Round-20 F1 addendum — PER-ROLE agent types, and the activeAgentIds semantics we had wrong.** Every MRR agent used to share one `type` (`mrr-overlay-v1`). Two engine mechanisms are keyed on agent TYPE and both broke under that: (a) per-chat activation — `perChatAgentSet` is built VERBATIM from `chatMeta.activeAgentIds` with no id→type mapping anywhere (`generate.routes.ts:1521-1522`) and gated `if (hasPerChatAgentList && !perChatAgentSet.has(cfg.type)) continue;` (`agent-resolution.ts:405`), so one shared type meant one toggle switched on all seven agents with no way to enable them individually; (b) injection placement (row 16) mints ONE token per type, consumed on first substitution, so even WITH a marker only the first of seven injections could land. **The finding that corrects this row's history:** `reconcileActiveAgents` has always pushed agent-config ROW IDS into `activeAgentIds`, and a row id can NEVER match `cfg.type` — the only other `perChatAgentSet` read (`:503`) iterates `BUILT_IN_AGENTS`, whose `.id` IS the type, so no id-keyed path exists. Our agents ran only because the ENGINE's own per-chat toggle writes TYPES (its list is built from manifests via `agentConfigsByType.get(a.id)`, `ChatSettingsDrawer.tsx:1485-1499`, and it appends that `agent.id`/type at `:3589`) — our pushed row ids were inert passengers whose ONLY effect was making `hasPerChatAgentList` true, which switches the per-chat filter ON. Fixed: the gmAgent keeps `mrr-overlay-v1` for continuity, additionalAgents get `mrr-overlay-v1-<role-slug>`, and reconcile now unions TYPES on both the forward and rebind paths (the rebind additionally drops the old ruleset's stale legacy row ids). Agent DISCOVERY is unaffected — `findManagedAgent`, the uninstall sweep, and the sole-writer filter all match on SETTINGS tags (`mrrManaged`/`mrrRulesetId`/`mrrAuthorId`/`mrrAgentRole`), never on `type`, which is also what makes discovery survive a type change. **⚠️ ROUND-21 CORRECTION — round-20's "in-place type migration" claim is RETRACTED; my own probe false-passed it.** `agentsStore.update()` (`agents.storage.ts:238-256`) builds `updateFields` from exactly `name`, `description`, `phase`, `connectionId`, `imagePath`, `promptTemplate`, and `settings`/`resultType` — **there is no `type` field in it at all**. A PATCH body carrying `type` is accepted and the type silently ignored, so reinstalling over existing agents leaves them on whatever type they already had; only a genuine delete-then-create can change it. Probe R20-F1-2 proved that `findManagedAgent` still locates each legacy agent by settings tags and that `mrrAgentTypeForRole` derives the right new type — it never asserted the PATCH persisted that type, so it passed while the claim it was cited for was false. Same class of false-pass this project has been burned by twice; the probe is re-scoped and renamed to what it actually demonstrates. **What is true, and is the anchor:** discovery via settings tags (`mrrManaged`/`mrrRulesetId`/`mrrAuthorId`/`mrrAgentRole`) works and is independent of type. **Additional hazard, verified:** `getUniqueCustomType` (`agents.storage.ts:124-136`) returns the requested type only when no row already holds it; otherwise it mints `<type>-<idSuffix>` and increments from there. A type string is therefore **not a stable identifier across reinstalls** — a delete-then-create that races or partially fails yields a suffixed type. Because round-20's preset marker sections key on the agent TYPE (row 16), a re-typed agent orphans its marker and silently recreates the read-path outage. **ROUND-28 — that deferred fix has landed.** `mrrReconcileAgentBindings` re-derives every binding from the SETTINGS TAGS (the only identity that survives a reinstall) rather than from the stored type: it resolves `role → current type` for the active ruleset, repoints orphaned marker sections at the live types (row 16), and clears `mrrResolveMutatorConfigId`'s per-ruleset row-id cache when the ruleset's managed rows change — that cache only re-resolved on an ACTIVE-RULESET change, which a reinstall is not, so it would otherwise have kept feeding the sole-writer filter a dead id. Fires after every bundle install, from `mrrConfirmChatRuleset`, and on a detected chat-preset apply; gated on `(chatId, rulesetId)` so the steady state is a string compare. |
| 4 | `/lorebooks` (+entries) CRUD | B | spellbook/install family | A0 live GET | ability-lorebook features warn+skip | Confirmed live 2026-08-22 — lorebook install + field-reference entry sync succeeded |
| 5 | `/chats/:id/metadata` (the `/chats/:id` customTrackerFields half is GONE) | B | mode detect; ruleset stamp + `activeAgentIds` round-trip | A0 live GET | none — the removed sheet sync never wrote anything | **ROUND-27 RESOLUTION — `syncSheetToChat` is DELETED, closing the round-21 correction.** Round 21 established that `PATCH /chats/:id` parses its body with `createChatSchema.partial().parse(...)` (`chats.routes.ts:1001`) and that `createChatSchema` (`chat.schema.ts:11-19`) declares exactly `{name, mode, characterIds, groupId, personaId, promptPresetId, connectionId}` — no `customTrackerFields`. A plain Zod object STRIPS unknown keys rather than rejecting them, so the request returned 200, stored nothing, and the field read back undefined. True at every engine version since 2.0.9, which is why the original "57-field sheet sync to chat succeeded" confirmation was retracted: it read an HTTP 200 as a write. Round 21 kept the code pending a design call; round 27 removed it along with its ~200-line field builder `buildSyncFields`, its toolbar button, and its leg of `scheduleAutoSync` — it was costing a GET plus a PATCH on every debounced save to do nothing. The sheets still reach the model exclusively through the agent promptTemplate injection (`syncSheetToAgents`) — since Round D that injection carries the **party block**: the active character's sheet plus every roster member's, not one sheet — which is why that path is load-bearing and was untouched; the managed-lorebook field-reference sync also stands. The designed replacement channel for this data is game-state `playerStats` (plan items B7/B8). The `/chats/:id/metadata` half of this row is unaffected and still works — it is how `activeAgentIds` and the ruleset stamp round-trip. **⚠️ ROUND-28 F4 — but the round-trip is NOT durable: applying an engine chat-preset REPLACES chat metadata wholesale.** `chat-presets.storage.ts` `applyToChat` (`:300-325`) builds `newMetadata = baseDefaults + presetMetadata + a short preserved allow-list + appliedChatPresetId`; `activeAgentIds`/`agentOverrides`/`agentPromptTemplateIds`/`customAgentImageSettings` are preserved ONLY when the preset itself doesn't carry that key, and **no `mrr*` key is on the list at all** — so `mrrChatRulesetId` is erased on every apply. Chat-preset metadata is also portable between chats (`CHAT_PRESET_EXCLUDED_METADATA_KEYS`, `chat-preset.ts:46-70`, excludes chat-specific keys but knows nothing about ours), so an apply can carry ANOTHER chat's stamp in. The stamp is therefore **advisory, not authoritative**: `mrrDeriveAndRestampChat` re-derives it from the chat's own managed agents (settings tags, which survive everything) whenever it is missing, and `mrrWatchAppliedChatPreset` detects an apply on a settled chat by watching the engine's own `appliedChatPresetId`. **ROUND-A ADDENDUM (2026-08-25, source-verified at v2.4.3 tag, live probes P-3/P-4 pending):** this row's erasure applies ONLY to chat-preset apply (allow-list, `chat-presets.storage.ts:296-325`). Chat BRANCHING copies metadata WHOLESALE minus a 5-key summary deny-list (`chats.routes.ts:3778-3781,3905-3916`) and game session-advance minus an 8-key list (`game.routes.ts:7185-7196`) — `mrr*` keys SURVIVE both. Scene creation builds fresh metadata (`scene.routes.ts:315-337`) — `mrr*` lost there. The stamp is more durable than this row previously implied. |
| 6 | `/regex-scripts`, `/custom-tools`, `/connections` | B | installer family | A0 live GET | affected installs warn+skip | `/regex-scripts` POST confirmed live 2026-08-22 — enforces `isPatternSafe()` (catastrophic-backtracking heuristic, `packages/shared/src/utils/regex-safety.ts`), rejects with 400 `"Regex pattern is unsafe..."` on unbounded quantifiers over broad classes (3+ anywhere, or 2 adjacent). Ported locally to `tools/lib/regex-safety.mjs`, wired into `npm run validate-bundles` so this is caught before import, not during it. `/custom-tools`, `/connections` still PENDING — live probe |
| 7 | `/chats/:id/game-state` (P2-A/B, future) | B | not yet called | probe when built | feature stays off | Not yet built (Phase B) |
| 8 | Sheet container + Attributes panel heading text | C | `findSheetContainer`, `hideBuiltInAttributesPanel` | smoke item | sheet floats unanchored + warning | Confirmed live 2026-08-22 — sheet renders/docks in both modes |
| 9 | Composer textarea + React synthetic input | C | `findChatInputTextarea` | smoke item | dice tags copy-to-clipboard + warning | Live bug found+fixed 2026-08-22: GM-mode composer (`GameInput.tsx`) is a strictly-controlled React textarea — direct `.value=` writes update React's value tracker so the input event diffs as no-change and the text silently vanishes (RP composer tolerated it, masking the bug). Fixed in two rounds: prototype-native value setter (controlled-input tracker bypass), then POSITIVE anchors after bottom-most-alone grabbed the Extensions panel's JS editor — primary selector is now `.marinara-chat-input-shell textarea` (all three mode composers carry the shell class; present since ≥v2.0.9), textarea-class fallbacks next, and the bare-textarea last resort excludes dialog-contained editors (null → clipboard degradation). Rolling AND send confirmed both modes 2026-08-22 (retest passed with settings panel open) |
| 10 | `[data-message-id]` DOM scraping | C | `processChatMessage` | smoke item | narrator path dead → poller becomes primary | Statically confirmed at v2.4.3 (`data-message-id` + `mari-message-assistant` present); live-UNEXERCISED — all live state flowed via the runs poller (now primary, the designed fallback posture of this row). **Round-17 addendum (TASK 1, live evidence: `swipe-detect: msgId=__streaming__ skipped — no tags, no buckets`):** `data-message-id` is NOT always a stable, unique, real identifier — the engine stamps a synthetic PLACEHOLDER id on the in-flight streaming message element while tokens are still arriving, reused across every in-flight generation on that surface, with no corresponding DB row. Confirmed against `~/Marinara-Engine` client source, exactly two literals exist (full-package grep, no others found): `"__streaming__"` (RP mode, `ChatRoleplaySurface.tsx:384`) and `"__conversation_live_stream__"` (the separate "conversation" chat mode, `ConversationView.tsx:903` — not yet seen live, included defensively since `ConversationMessage.tsx` stamps the identical `mari-message-assistant` class + `data-message-id` attribute this row's selectors match on). Game mode's `GameSurface.tsx` renders its own message list with neither `ChatMessage` nor `ConversationMessage` — no placeholder risk there, still untouched (game mode remains generally untested for this row, per round-16's note below). Server-side confirmed NOT at risk: every `agentsStore.saveRun(...)` call site passes a real, persisted messageId — never a placeholder literal — so row 2's poller transport can never receive one; this is purely a DOM-path concern. Fix: `mrrIsPlaceholderMessageId` gates `schedule()`/`processChatMessage()`/the initial-sweep loop before any state is touched for one of these ids — see the CHANGELOG round-17 entry for the full hunk. **Round-17 addendum #2 (TASK 2):** this row's `processChatMessage` apply path was found to have NEVER received round-16's journal-loaded-before-apply protection (only row 2's poller got it) — a real asymmetry, now fixed symmetrically; see row 2's own round-17 addendum for the shared mechanism. |
| 11 | `localStorage["marinara-active-chat-id"]` (engine's own key) | C | `getChatId` | smoke item | URL-regex fallback (exists) + warning if both fail | Confirmed live 2026-08-22 — chat id resolution worked throughout (ruleset activation, sync, poller all keyed on it) |
| 12 | Peek Prompt (Game Logs) as verification surface | C | manual smoke only | smoke item | verify via agent promptTemplate instead | Confirmed live 2026-08-22 via the promptTemplate path itself: all 7 managed agents carry exactly one injected block; single constant field-reference lorebook entry. **Round-D correction:** that block is the PARTY block (active + every roster member with a record for this ruleset), not a single sheet — "one block per agent" is still exact, "one sheet" never was after Round D. |
| 13 | `document.head`/`document.body` injection points | C | `init` (style inject), dialog/panel mounts | smoke item | none (page-fundamental) — note only | Page-fundamental, no fallback needed |
| 14 | CSRF protection hook on unsafe `/api` methods | B | compat shim `apiFetch` + `apiPostRaw`/`apiDeleteRaw` | A0.5 unsafe-method probe | writes fail loudly with panel error naming the CSRF header | `CSRF_HEADER`/`CSRF_HEADER_VALUE` confirmed 2026-08-22 against engine `v2.4.3` `packages/shared/src/constants/security.ts`; header now sent unconditionally on non-GET/HEAD by the shim + both raw helpers — **live 403/200 confirmation still PENDING** (plan §4.A0.5) |
| 15 | `GET /chats/:chatId/messages?limit=N` (+ `activeSwipeIndex` field dependency) — B2 swipe-aware processed-message keying | B | `fetchSwipeIndexMap`/`resolveSwipeIndexForMessage`, called from `processChatMessage`, `extension/RPG-Extension-GM-Mode.js` (~line 13440, module region after `saveProcessedMessageIds`) | probe = the batched fetch itself, coalesced per scan cycle via a shared in-flight promise, only fired for tag-bearing candidates not already suppressed by a legacy plain-msgId entry | fallback = plain-messageId keying (pre-B2 behavior) — on ANY fetch failure (rejected/thrown, or a non-array response body), or when a candidate's id is absent from the fetched recent-window (`?limit=50`), that message keys/dedupes by plain `msgId` for the cycle; one `console.warn` per session on fetch failure | Statically confirmed 2026-08-22 against engine source: route `packages/server/src/routes/chats.routes.ts` `GET /:id/messages` (registered under `/api/chats` prefix, `routes/index.ts:65`) — unpaginated (`storage.listMessages`) when no `limit` query param, paginated latest-N (`storage.listMessagesPaginated`) when `?limit=N` is given. Row shape is the full `messages` table row (`db/schema/chats.ts:44-56`) plus `rowid`/`swipeCount`; confirmed field `activeSwipeIndex: integer("active_swipe_index").notNull().default(0)` (`:52`) — camelCase in the JS row object. A swipe does NOT mint a new message id; it changes this field on the SAME message row (alternate content lives in the separate `messageSwipes` table) — this is the reason plain-msgId keying swallowed swipe 1+ pre-B2. No mode branch exists in `processChatMessage`/`watchChatMessages` — the fetch + composite keying applies uniformly in both Game and RP mode (no per-mode gate found to hook into; see CHANGELOG B2 entry for the design-fidelity note this raises against the 07-15 plan's "RP mode: unchanged semantics" line). **ANCHOR DECISION (round 5, FIX-1, critical fix):** the composite `msgId:swipeIndex` key is used ONLY for the `processedMessageIds` entry (whole-message+swipe reprocessing gate) — it must NEVER be passed as `applyStateTagsWithDedup`'s cross-transport anchor, because `extractRunAnchor` (the runs-poller transport) has no swipe-index concept and only ever returns plain `msgId`; passing the composite key there desynced the two transports' anchors and let identical tags apply twice. Both transports anchor on plain `msgId` again. Accepted consequence: identical-content tags on two different swipes of the same message now dedup to one apply (protective); different-content tags on different swipes still each apply, stacking both swipes' mutations — this "stacking" behavior is a known open design question logged for the maintainer, not resolved by this fix. **UPDATE (round 7, B2-R swipe-revert; ⚠️ round-10 doc emphasis — read before relying on this for Exalted-class systems):** stacking is now resolved for the SCALAR-field subset a mutation journal can revert (attributes/skills/derived stats via the generic numeric path, ruleset-declared states) — a swipe change reverts the prior swipe's journaled scalar entries before the new swipe's tags apply. **Revert covers numeric fields and states, and — as of round 11 (T2) — typed-damage tracks (Exalted's bashing/lethal/aggravated health boxes and any other `renderAs:"track"` derived stat), journaled as a per-cell array snapshot under field `"__mrrTrack:" + trackName` and replayed via the internal `__mrrTrackRestore` sentinel (the legacy typed-counter `sheet.track[trackName]` is fully re-derived from the cells snapshot by `syncTrackCellsToTyped`, so it is not separately snapshotted), and — as of round 11 (T2-EXPANDED) — conditions add/remove, journaled as a whole-array snapshot under field `"__mrrConditions"` and replayed via the internal `__mrrConditionsRestore` sentinel (a wholesale array replace, not a re-run of the original add/remove op — sidesteps a later unrelated mutation in the same bucket resurrecting/re-deleting an earlier entry's target). Array mutations (inventory/backgrounds/intimacies/xp/attunement/investiture/commitment) remain NOT journaled at all. Flagged for extension, queued for a follow-up session — not solved this build.** Array/object-shaped mutations (inventory, backgrounds, intimacies, xp, attunement/investiture/commitment) are OUT OF the journal's scalar-shaped declared shape ({field,kind:"set"}) and still stack exactly as before — see `extension/RPG-Extension-GM-Mode.js`'s B2-R header comment (~`saveProcessedMessageIds` region) for the full scope boundary. Journal detail: local-only, `LS_MUTATION_JOURNAL_PFX + chatId`, never synced (same per-browser observation class as the processed-message/run sets); no chat-lifecycle GC exists for any of the three prefixes (not invented here, per instruction) — see the journal's own header comment for the audit. **Round-18 addendum — this row's response now feeds a SECOND consumer and depends on three more fields.** The same `?limit=50` fetch also builds the run-anchor map that fixes row 2's orphaned buckets (`mrrBuildMsgAnchorMap`, called from `fetchSwipeIndexMap`'s own `.then` — no extra request, same coalescing). Fields now read beyond `id`/`activeSwipeIndex`: **`role`** (`db/schema/chats.ts:49`, enum `user` / `assistant` / `system` / `narrator`) to tell an assistant reply from a user turn; **`rowid`** (`chats.storage.ts:1227`, 1-based ABSOLUTE position, consistent across both the `listMessages` and `listMessagesPaginated` server paths) as the primary sort key, so ordering is normalized rather than trusted from array position; and **`extra`** (JSON string) to skip `hiddenFromUser: true` messages, which the client never renders. Fallback: a response with no `role` on any row marks the anchor map unusable and row 2's poller reverts to its pre-round-18 raw-messageId anchoring with one warn — row 15's own swipe-index keying is unaffected either way. **Round-19:** ordering verified at the source rather than assumed — `listMessagesPaginated` (`chats.storage.ts:1236-1270`) selects `desc(createdAt), desc(id)` then `.reverse()`s, so a `?limit=N` response is the newest N in ASCENDING order carrying a correct ABSOLUTE `rowid` (`upperRowid - rowsDescending.length + index + 1`); the unpaginated `listMessages` (`:1221-1232`) orders `createdAt, id` ascending with `rowid` as a 1-based index. Both agree with the `rowid`-primary sort in `mrrBuildMsgAnchorMap`, and a just-inserted assistant row is always present and always sorts last. FIX-13a (minor, same commit): `fetchSwipeIndexMap`'s shared in-flight promise now also tracks which chatId it belongs to — a caller for a DIFFERENT chat (mid-cycle chat switch) no longer reuses another chat's in-flight fetch result; it falls back to the same null/plain-msgId degrade path a fetch failure already uses. |
| 16 | `/api/prompts` preset + section routes (`GET /:id/full`, `GET /default`, `POST /:id/sections`, `PATCH /:presetId/sections/:sectionId`) + the `agent_data` marker contract, AND the v2.4.0 preset-owns-placement semantics | B | `mrrAddAgentSectionsToActivePreset`, `mrrResolveActivePreset`, `mrrBuildAgentSectionBody`, `mrrExistingAgentSectionTypes`, `mrrInjectableManagedAgents` (round 20 F3); `mrrReconcilePresetMarkers`, `mrrRoleForOrphanType` (round 28 F3) | the `GET /prompts/:id/full` read itself; a 409 on section POST identifies the read-only stock preset | every failure is surfaced to the user in the dialog and nothing is written: no preset on the chat and no default ⇒ refuse with instructions; 409 ⇒ "save as copy" guidance; no injectable agents ⇒ tell the user to re-install the bundle. The extension never silently edits a preset, and never writes without a confirm naming it | **THE READ-PATH COUPLING — this is how agent output reaches the narrator at all on 2.4.0+.** A pre_generation agent's context injection is delivered ONLY by substituting a token minted for an `agent_data` marker section whose `markerConfig.agentType` equals the agent's TYPE (`generate.routes.ts:2465-2492`). If nothing matches and the preset assembler ran, `splitRuntimeHandledAgentInjections` is called with `omitUnmatched: presetOwnsAgentPlacement` (`generate.routes.ts:5274`; flag set true at `:2589`) and the output is moved to `omittedInjections`, then dropped (`:5276-5278`) — **silently, with no depth-0 fallback**; the fallback exists only when the assembler did NOT run. Net effect before round 20: agents ran, billed tokens, and wrote healthy-looking rows into `agent_runs` while contributing nothing to the prompt. Regression window: commit `91ba44fd2`; unlike long-term-memory, which has an explicit escape hatch at `:5443`, custom agents have none. **Shape facts, verified against source (two of them contradict what a GET appears to show):** the route prefix is `/api/prompts` (`routes/index.ts:70`) — NOT `/api/presets`; `/api/chat-presets` is an unrelated subsystem. READS return `enabled`/`isMarker` as the STRINGS `"true"`/`"false"` and `markerConfig` as a JSON STRING (`prompts.storage.ts:329-335` does no deserialization), but WRITES demand real booleans and a real object — `createPromptSectionSchema` uses `z.boolean()` with no coercion (`prompt.schema.ts:170-188`), so posting `"true"` is a 400. `isMarker` and `identifier` are immutable after creation (omitted from `updatePromptSectionSchema`). There is no bulk section write: POST one at a time (each appends to `preset.sectionOrder`, `prompts.storage.ts:360-366`). We deliberately do NOT call `PUT /:id/sections/reorder`, because it rewrites EVERY section's `injectionOrder` to `i*100` and would reshuffle a hand-tuned preset. The section body mirrors `PresetEditor.tsx:1265-1294` exactly, including `content: "{{agent::<type>}}"` — the assembler DISCARDS a marker section whose content is empty (`assembler.ts:844-890`). Markers are ROLEPLAY-ONLY (`generate.routes.ts:2457` excludes conversation and game). A chat with `promptPresetId === null` uses no preset at all — the server has no default fallback, so the assist offers to attach `GET /prompts/default` first. Eligibility for a marker to actually fill requires the agent's TYPE in `activeAgentIds`, `phase === "pre_generation"`, and `resultType === "context_injection"` (`runtime-agent-sections.ts:117-124`); the Preset Editor's own picker checks only `settings.injectAsSection === true` (`PresetEditor.tsx:1245-1254`, strict boolean) and does NOT check phase or resultType — a real divergence that lets a user add a section the server will never fill, so we apply the server's rules ourselves. Probes: `r20-agents-probes.mjs` R20-F3-1..F3-6 plus the R20-FUNNEL set. **ROUND-28 ADDENDUM — markers are now RECONCILED, not just created.** Because agent types are not stable across reinstalls (row 3), a marker section can outlive the agent it names. `mrrReconcilePresetMarkers` PATCHes such a section back onto the live type via `PATCH /prompts/:presetId/sections/:sectionId` (`prompts.routes.ts:366-377`), rewriting `content` and `markerConfig.agentType` **together** — the assembler resolves the body macro through `ctx.macroCtx.agentData[markerConfig.agentType]` (`assembler.ts:845`, `:878`), so a mismatch between the two produces an empty section. `identifier` is left stale on purpose: `updatePromptSectionSchema` omits it (`prompt.schema.ts:186-188`) and nothing in marker resolution reads it. Sections whose type IS live are untouched, and a type that does not parse against `mrrAgentTypeForRole`'s scheme + the engine's collision-suffix grammar is never touched at all (third-party sections are out of bounds). A 409 (stock read-only preset) becomes a one-time warn, not an install failure. |
| 17 | **LLM provider tool-calling capability** — the engine's tool loop is gated on `provider.chatComplete`, which the SUBSCRIPTION providers do not implement, so `roll_dice` is undeliverable on them even with Function Calling ON. Plus `GET /connections` row fields `provider` / `isDefault` / `fallbackForMain`, and the chat row's `connectionId`. | C | `MRR_TOOLLESS_PROVIDERS`, `mrrNormalizeProviderId`, `mrrProviderIsToolless`, `mrrPickNarratorConnection`, `mrrPickMainFallbackConnection`, `mrrDiceIntegrityNoticeText`, `mrrCheckDiceToolCapability` (D-2 guard, `extension/RPG-Extension-GM-Mode.js`); harvest in `mrrNoteChatRow`; fired from `mrrConfirmChatRuleset` | `dice-integrity-probes.mjs` (eval-extracted: denylist match incl. the underscore/hyphen fold, once-per-chat, call-count-across-turns, fail-open on unknown/unresolvable/read-failure) | **Fail-open and display-only.** Unknown provider, unresolvable connection, or a failed `/connections` read → SILENT; no pipeline is altered in any case. The doctrine half is independent of the guard: all 16 `gm-agent.md` files now carry the no-tool fallback clause, so a narrator on a toolless connection delegates rolls to the player's widget whether or not this notice ever renders. | Confirmed 2026-08-25 against engine `HEAD 2.4.3-dev` by source read (no live probe needed — this is a code-shape fact, not a runtime one): `packages/server/src/services/llm/providers/claude-subscription.provider.ts` and `grok-subscription.provider.ts` contain **0** refs to `chatComplete`, while `anthropic` / `google` / `openai` / `openai-chatgpt` / `local-sidecar` all implement it; the gate is `packages/server/src/routes/generate.routes.ts:6351` — `if (enableChatTools && provider.chatComplete) {`. **RE-VERIFY EVERY ENGINE RELEASE** (§9 triage): if a subscription provider gains `chatComplete`, this denylist starts lying in the SAFE direction (a spurious notice), but if a NEW toolless provider ships it lies in the DANGEROUS direction (silent fabrication) — the list is a denylist, not a capability probe, because the capability is not observable from the client. **Wire-format trap:** `apiProviderSchema` (`packages/shared/src/schemas/connection.schema.ts:8-23`) spells these `claude_subscription` / `grok_subscription` with UNDERSCORES; the constant keeps the hyphenated provider-FILE spelling and `mrrNormalizeProviderId` folds `_`→`-`, so a future edit that drops the fold silently kills the guard. |
| 18 | ⚠️ RETRACTED-AND-CORRECTED 2026-08-28 (Cato audit): ~~an agent created via the API WITHOUT this object receives ALL sources~~ — that fallback branch is unreachable for MRR rows. `getAgentContextSources` (`agent-executor.ts:125-131`) returns ALL sources only for BUILT-IN agents with no object; every MRR row is `isCustomAgent: !BUILT_IN_AGENTS.some(a => a.id === cfg.type)` (`agent-resolution.ts:503`) and therefore ALWAYS normalizes to the custom default = **chatHistory-only** when the object is unset (`types/agent.ts:583-603`; unknown keys ignored). Consequences: (a) MRR agents pre-round ran chatHistory-only — they never saw lorebook/characters/persona; (b) the original Stage-2C table was a context GRANT, not a trim — replaced 2026-08-28 with explicit PARITY stamps (chatHistory-only + PIT contextSize:4) pending a Corey ruling on grants; (c) an MRR agent shipping without the object reverts to chatHistory-only, not ALL. Real source enum: chatHistory, characters, persona, activatedLorebookEntries, chatSummary, authorNotes, trackerData, recalledMemories ("others" is not a key). | C | engine `types/agent.ts:570-603`, `agent-executor.ts:125-131`, `agent-resolution.ts:503` | source read + audit-verified (static analysis, no live probe) | Parity stamp = behavior-neutral by construction; heal preserves an existing object byte-for-byte (stage 5a) | confirmed 2026-08-28 — staging v2.4.3-350; stale-recheck at live 2.4.4 |
| 19 | Agent batching — the engine batches queued agent invocations per (phase × provider+model × request-signature) rather than firing one request per agent. Batched calls get ONE context whose sources are the **OR-union across members** (`getBatchContextSources`, `agent-executor.ts:132-143`) — so a per-role contextSources grant is effectively group-wide within a same-connection phase group (Cato audit 2026-08-28; governs any future grant ruling). ⚠️ minor correction 2026-08-28: ~~the tier map changes each agent's request signature~~ — the signature is settings-based (temperature/caching/maxTokens, `:587-598`), NOT prompt-content-based; tier changes do not affect batch grouping | C | `agent-pipeline.ts:81,292`, `agent-executor.ts:132-143,587-598` | source read + audit-verified (static analysis, no live probe) | Informational — no MRR code branches on batching | confirmed 2026-08-28 — staging v2.4.3-350; stale-recheck at live 2.4.4 |
| 20 | Per-chat `lorebookTokenBudget` — two-layer eviction (chat-level budget, then lorebook-level) governs which activated lorebook entries survive into an agent's context | C | `chat.ts:433`, `lorebook/index.ts:651-663,692-694` | source read this session (static analysis, no live probe) | Informational this round — relevant to context-fuser/essence-manager's `activatedLorebookEntries` source (Stage 2C table) if entry eviction under budget pressure needs revisiting later; no MRR-side fallback exists or is needed for this round's data-only settings change | confirmed 2026-08-27 — staging v2.4.3-350; stale-recheck at live 2.4.4 |
| 21 | **Agent template appears TWICE per call** — the engine's assembly includes each agent's full rendered `promptTemplate` (incl. anything MRR injected into it, i.e. the sheet block) once in the system `<agents>` section AND once again, whole, in the Output Format block ("Active agent output contracts" — `buildAgentOutputFormatBody` pushes `getAgentOutputTemplate(...)` per config). Applies to BOTH solo (`:2096` + `:2126`) and batch paths. Engine-intended design, not a bug. Consequences: per-call wire cost ≈ 2× stored template size; every byte trimmed from stored templates (tiering, mutator diet) saves double; likely resolves the old "×1.75 banner tokenizer" hypothesis (banner ≈ per-turn assembled WITH duplication; the Custom Agents panel ≈ stored size, o200k-scale). Surfaced by Corey's live narrator diagnostic 2026-08-28, verified in source | C | `agent-executor.ts:460-471, 507-513, 2085-2131` | source read + live-diagnostic corroboration | None needed — informational; feeds the mutator-diet and template-lean levers | confirmed 2026-08-28 — staging v2.4.3-350; stale-recheck at live 2.4.4 |

## PARTY-WRITES ADDENDUM (2026-08-25) — the applier ROUTES by `target=`

**This addendum SUPERSEDES every earlier statement in this repo, this file included,
that `applyStateTagsWithDedup` ignores a `[mrr-state:]` tag's `target=` attribute.**
That was true through Round E and it is the reason the D-3 mutator clause existed;
it is false as of this build. Anything still claiming it is stale — treat this
paragraph as the authority and fix what you find.

**What changed.** A tag's `target=` is resolved to a canonical charId before the
content signature is computed, and the write is applied to that character's own
composite record (`mrr-character-<charId>@<rulesetId>`) inside a synchronous binding
that swaps `state.sheet` / `state.activeCharacterId` and restores both in a
`finally`. Mira's damage moves Mira's sheet while Tester is active. Full design:
`AGENTS.md` §5.6.

**No new engine coupling.** This is the load-bearing claim for this ledger: party
writes add **zero** ledger rows and touch **zero** new engine surfaces.

- Target resolution is against OUR roster (`state.characters` via
  `mrrPartyRosterOrder`) — never a name-keyed engine lookup.
- Records are the Round-A composite localStorage keys, already covered by row 1b's
  storage adapter. No key shape changed, no schema migration.
- The journal is unchanged in shape and location (`LS_MUTATION_JOURNAL_PFX + chatId`,
  local-only); entries merely gained `charId` and `v: 2`.
- Both transports (row 2's runs poller, row 10's DOM scrape) call
  `applyStateTagsWithDedup` with the identical arguments they always did — routing
  lives entirely inside that function and below. Row 15's plain-`msgId` anchor
  invariant (FIX-1) is untouched.
- No new fetches or polling: the roster and the records are already local.
- The mutator's routing clause is generated at the sync layer and injected only into
  the `state-mutator` agent's promptTemplate — the same D-3 mechanism, so no ruleset
  file is edited and the agent-prompt coupling surface is unchanged.

**Row 2 / row 15 consequence worth recording.** Round 23's bucket-level sole-writer
claim is now keyed per `(bucket, target)`. That closes the accepted-loss case the
row-2 telemetry header asked to be reported with real data: log ex30642:132 showed
one bucket carrying the active character's Anima Banner write on one channel and a
distinct `target=` motes write on the other, and the bucket-level claim suppressed
the second — a genuinely distinct write lost. Per-character keying admits it.
Within one character, the cross-model echo suppression that trade-off bought is
unchanged.

**Sole-writer invariant (plan §1.3) still holds.** The state-mutator, via narrator
tags and the deduped runs poller, remains the ONLY sheet writer. Party writes add no
second live writer — they change WHICH record the one writer addresses.

## Data-contract versions (seeded, plan §5.B14)

- `stateTagVersion` (the `[mrr-state:]` grammar): **stamped, round 7** — `MRR_STATE_TAG_VERSION = 1` (`extension/RPG-Extension-GM-Mode.js`, declared beside `STATE_TAG_RE`/`STATE_KV_RE`). One constant, no behavior change; not yet read/enforced by any parser or migration path — this closes the B14 leftover ("not yet stamped") but does not itself add grammar versioning logic.
- Ruleset schema version: `bundle.json` `version: 1` (existing).
- **B18 schema duplication hazard (2026-08-23, no engine coupling — internal maintenance note):** `schema/ruleset.schema.json`'s `resolution.oneOf` has no `$defs`/`definitions` section; all nine mode configs are inlined directly in that array (pre-existing, unchanged by B18 — restructuring it into $ref form was judged out of scope for a same-night, zero-regression build with 16 rulesets already validating against it). `additionalModes[].config` reuses each mode's field SET via nine new `$defs.resolutionConfig*` entries that DUPLICATE (not $ref) the corresponding `resolution.oneOf` branch's properties/required, minus the `mode` const. **Whoever next changes a resolution mode's fields in `resolution.oneOf` must update the matching `$defs.resolutionConfig*` by hand** — there is no schema mechanism enforcing the two stay in sync; see the `$comment` on `$defs.additionalModesArray` for the full rationale.
- Storage layout version: one record, one top-level key per `localStorage` key string, mirroring existing key strings exactly. Each value is a `{t, v}` envelope (round 4/5 — see row 1b): `t` an epoch-ms number, `v` a string or `null` (tombstone). There is no explicit version field on the envelope or the record — shape is duck-typed on read via `mrrNormalizeEnvelope` (valid envelope ⇒ finite numeric `t` + string-or-null `v`; anything else degrades to `{t: 0, v: null}` or, for a bare raw string, `{t: 0, v: <string>}`), which is also what lets a pre-envelope raw value (should one ever exist) or a hand-edited/corrupted record be read safely without a migration step.
